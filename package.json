{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "install": "lerna bootstrap --hoist",
    "deploy": "lerna run deploy --stream"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
